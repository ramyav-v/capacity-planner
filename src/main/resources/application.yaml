spring:
  data:
    rest:
      base-path: /capacityplanner/api
  datasource:
    url: jdbc:mysql://localhost:3306/capacity_planner
    username: root
    password: P@55word
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080

jwt:
  secret: capacity-planner-jwt-secret-key-must-be-at-least-32-chars
  expiration: 86400000